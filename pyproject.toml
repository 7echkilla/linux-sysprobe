[project]
name = "probe"
version = "0.0.1"
description = "Linux system diagnostic tool"
requires-python = ">=3.8"
dependencies = [
    "typer",
    "psutil"
]

[tool.setuptools]
packages = ["probe"]

[project.scripts]
probe = "probe.cli:app"